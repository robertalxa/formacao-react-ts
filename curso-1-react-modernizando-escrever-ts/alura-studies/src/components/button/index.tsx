import { Component } from 'react';

class Button extends Component {
    render() {
        return (
            <button>
                Botão
            </button>
        )
    }
}

export default Button;